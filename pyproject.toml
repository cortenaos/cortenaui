[project]
name = "cortenaui"
version = "0.3.0-alpha"
description = "Modular UI components for Jetpack Compose"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "zensical>=0.0.40",
]
